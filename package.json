{
  "version": "1.0.1",
  "scripts": {
    "build": "docker build --tag wireguard .",
    "dev": "docker-compose -f docker-compose.yml up --build --force-recreate",
    "start": "docker-compose -f docker-compose.yml up --build --force-recreate -d"
  }
}
